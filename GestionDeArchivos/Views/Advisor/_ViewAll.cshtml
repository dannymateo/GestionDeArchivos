@model IEnumerable<GestionDeArchivos.Data.Entities.Advisor>

<table class="table table-hover table-striped" id="example2">
    <thead style="background-color: #292D5F;color:white;">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DocumentsNumber)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DocumentsNumber)
                </td>
                <td>
                    <a onclick="showInPopup('@Url.Action("AddOrEdit", "Advisor", new { id=item.Id },
Context.Request.Scheme)' , 'Actualizar asesor')" class="btn btn-sm btn-warning"><i class="fa fa-pencil-alt"></i> </a>
                    <button data-id="@item.Id" class="btn btn-sm btn-danger deleteItem" data-toggle="modal"
                        data-target="#deleteDialog">
                        <i class="fa fa-trash"></i>
                    </button>
                </td>
            </tr>
        }
    </tbody>

</table>